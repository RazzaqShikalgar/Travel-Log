<!DOCTYPE html>
<html>
  <head>
    <%- include('common/head'); %>
  </head>
  </head>
  <body>
    <%- include('common/nav'); %>
      <!-- <form onsubmit="return f(this);" action="http://localhost:3000/api/blogs" method="POST" class="trip-form"> -->
        <div class="full-container mx-5">
          <div class="col-lg-12 my-5 d-flex">
            <h1 id="buttons" class="mr-auto">Create</h1>
            <button class="btn btn-primary ml-auto" style="margin-left: 48px;width:200px; margin-top: 3px;"  onclick="f()">Post</button>
          </div>
          <div class="row">
            <div class="col-md-4">
              <%- include('components/createBlog.ejs'); %>
            </div>
            <div class="col-md-8">
              <%- include('components/blog.ejs'); %>
            </div>
          </div>
        </div>
      <!-- </form> -->
  </body>
  <script>
  
    var file = document.getElementById("formFile").files[0];
    var formData = new FormData();
    formData.append("image", file);
    function f() {
      // let req = {};
      // formData.forEach(function(value, key){
      //   req[key] = value;
      // });
      fetch('http://localhost:3000/api/blogs', {
        method: 'post',
        body: formData
      }).then(function (res) {
        if (res.ok) {
          console.log(res);
        } else {
          console.log("error", res);
        }
      });
      console.log("Down here");
      return false;
    }
  </script>
</html>